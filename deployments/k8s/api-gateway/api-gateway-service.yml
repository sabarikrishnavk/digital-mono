apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: digital-mono
spec:
  selector:
    app: api-gateway
  ports:
    - protocol: TCP
      port: 80 # External port
      targetPort: 8080 # KrakenD container port
  type: LoadBalancer # Exposes the API Gateway externally
