apiVersion: 1

providers:
  - name: 'default'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards # Path where JSON dashboards are in docker-compose
      # For K8s, we might use /var/lib/grafana/dashboards or similar
